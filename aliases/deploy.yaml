---
name: "deploy"
action_ref: "st2-pack-omnia.deploy"
description: "deploy"
formats:
  - "deploy environment {{ environment }} stack {{ stack }} sha {{ sha }}"
  - "deploy environment {{ environment }} stack {{ stack }} branch {{ sha }}"
  - "deploy environment {{ environment }} stack {{ stack }} ref {{ sha }}"
