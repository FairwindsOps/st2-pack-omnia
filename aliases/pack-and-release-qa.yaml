---
name: "pack-and-release-qa"
action_ref: "st2-pack-omnia.pack-and-release-qa"
description: "pack and release qa"
formats:
  - "pack and release to qa stack {{ stack }} sha {{ branch }}"
  - "pack and release to qa stack {{ stack }} branch {{ branch }}"
  - "pack and release to qa stack {{ stack }} ref {{ branch }}"
